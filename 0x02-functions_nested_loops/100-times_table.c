#include "main.h"
/**
 * print_times_table - prints the n times table
 * @n: number of the times table
 */
void print_times_table(int n)
{
	int l, m, o;

	if (n >= 0 && n <= 15)
	{
	for (l = 0; l <= n; l++)
	{
	for (m = 0; m <= n; m++)
	{
	o = m * n;
	}
	}
	}
}
Footer
© 2022 GitHub, Inc.
